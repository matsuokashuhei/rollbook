<%= render "shared/notice" %>
<div class="panel panel-default">
  <div class="panel-body">
    <%= render "form" %>
    <%= link_to t("views.buttons.back"), member_members_courses_path(@member), class: "btn btn-default pull-left" %>
    <%= link_to t("views.buttons.edit"), edit_member_members_course_path(@member, @members_course), class: "btn btn-default pull-right" %>
  </div>
</div>
