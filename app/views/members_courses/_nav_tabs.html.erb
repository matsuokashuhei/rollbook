<ul class="nav nav-tabs">
  <li>
    <%= link_to t("activerecord.models.member"), member_path(@member), class: "btn btn-link" %>
  </li>
  <li class="active">
      <a href="#members_courses" data-toggle="tab"><%= t("activerecord.models.members_course") %></a>
  </li>
  <li>
    <% if current_page?(member_course_path(@member, @members_course)) %>
      <%= link_to t("activerecord.models.lesson"), member_course_rolls_path(@member, @members_course), class: "btn btn-link" %>
    <% else %>
      <%= link_to t("activerecord.models.lesson"), member_rolls_path(@member), class: "btn btn-link" %>
    <% end %>
  </li>
  <li>
    <% if current_page?(member_course_path(@member, @members_course)) %>
      <%= link_to t("activerecord.models.recess"), member_course_recesses_path(@member, @members_course), class: "btn btn-link" %>
    <% else %>
      <%= link_to t("activerecord.models.recess"), member_course_recesses_path(@member, @members_course), class: "btn btn-link", disabled: "disabled" %>
    <% end %>
  </li>
  <li>
    <%= link_to t("activerecord.models.bank_account"), member_bank_account_path(@member), class: "btn btn-link" %>
  </li>
  <% if current_page?(member_courses_path) %>
    <%= link_to t("views.buttons.new"), new_member_course_path, class: "btn btn-default pull-right" %>
  <% end %>
</ul>


