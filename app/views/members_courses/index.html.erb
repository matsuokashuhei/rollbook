<div class="row">
  <div class="col-md-12">
    <ol class="breadcrumb">
      <%= list_item_to_members %>
      <%= list_item_to_member(@member, active: true) %>
    </ol>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <ul class="nav nav-tabs">
      <%= tab_to_member(@member) %>
      <%= tab_to_members_courses(@member, active: true) %>
      <%= tab_to_members_rolls(@member) %>
      <%= tab_to_members_recesses(@member) %>
    </ul>
  </div>
</div>
<br />
<div class="row">
  <div class="col-md-12">
    <%= form_tag member_members_courses_path(@member), method: :get, class: "form-inline" do %>
      <div class="form-group">
        <%= select_tag 'status', options_for_select([['在籍', '1'], ['退籍', '9']], params[:status]), class: "form-control", include_blank: true %>
      </div>
      <%= button_to_search %>
      <%= button_to_new new_member_members_course_path(@member), pull: "right" %>
    <% end %>
  </div>
</div>
<br />
<div class="row">
  <div class="col-md-12">
    <%= render "shared/notice" %>
    <div class="table-responsive">
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th><%= t("activerecord.models.studio") %></th>
            <th><%= t("activerecord.attributes.timetable.weekday") %></th>
            <th><%= t("activerecord.models.time_slot") %></th>
            <th><%= t("activerecord.models.course") %></th>
            <th><%= t("activerecord.attributes.members_course.begin_date") %></th>
            <th><%= t("activerecord.attributes.members_course.end_date") %></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @members_courses.each do |members_course| %>
            <% course = members_course.course %>
            <% timetable = course.timetable %>
            <% time_slot = timetable.time_slot %>
            <% studio = timetable.studio %>
            <% if members_course.end_date.present? %>
              <tr class="active">
            <% else %>
              <tr>
            <% end %>
              <td><%= studio.name %></td>
              <td><%= timetable.weekday_ja %></td>
              <td><%= time_slot.start_time %></td>
              <td><%= course.name %></td>
              <td><%= members_course.begin_date %></td>
              <td><%= members_course.end_date %></td>
              <td>
                <%= link_to_show member_members_course_path(@member, members_course) %>
                <%= link_to_edit edit_member_members_course_path(@member, members_course) %>
                <%= link_to_destroy members_course.id, member_members_course_path(@member, members_course), destroy: members_course.destroy? %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
