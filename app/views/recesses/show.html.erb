<%= render "breadcrumb" %>
<%= render "nav_tabs" %>

<div class="tab-content">
  <br />
  <%= render "form" %>
  <%= button_to_edit edit_member_recess_path(@member, @recess), pull: "right" %>
  <%= button_to_back member_recesses_path(@member), pull: "left" %>
</div>
