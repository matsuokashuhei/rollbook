<%= render "nav_tabs" %>

<div class="tab-content">
  <br />
  <%= render "breadcrumb" %>
  <%= render "form" %>
  <%= link_to t("views.buttons.edit"), edit_member_course_recess_path(@member, @members_course, @recess), class: "btn btn-default pull-right" %>
  <%= link_to t("views.buttons.back"), member_course_recesses_path(@member, @members_course), class: "btn btn-default pull-left" %>
</div>
