<%= render "breadcrumb" %>

<div class="tab-content">
  <br />
  <%= form_tag({ action: "trial" }, { method: "post", id: "form" }) do %>
    <div class="table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th><%= t("activerecord.attributes.member.number") %></th>
            <th><%= t("activerecord.attributes.member.name") %></th>
          </tr>
        </thead>
        <tbody>
          <% @rolls.each do |roll| %>
            <tr>
              <td><div class="checkbox"><%= check_box_tag "rolls[][member_id]", roll.member_id %></div></td>
              <td><%= roll.member.number %></td>
              <td><%= roll.member.name %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <%= back_link_to edit_lesson_rolls_path(@lesson), pull: "left" %>
    <%= button_tag t("views.buttons.select"), class: "btn btn-default pull-right", form: "form" %>
  <% end %>
</div>
