<div class="row">
  <div class="col-md-6">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>#</th>
          <th><%= t "activerecord.models.recess" %></th>
          <th><%= t "activerecord.models.member" %></th>
        </tr>
      </thead>
      <tbody>
        <% @recesses.each.with_index(1) do |recess, i| %>
          <% members_course = recess.members_course %>
          <% member = members_course.member.decorate %>
          <% course = members_course.course %>
          <tr>
            <td><div class="pull-right"><%= i %></div></td>
            <td><%= link_to course.name, member_recess_path(member.id, recess.id) %></td>
            <td><%= link_to member.name, member_path(member) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>


