<div class="row">
  <div class="col-md-12">
    <ol class="breadcrumb">
      <%= list_item_to_courses(@studio) %>
      <%= list_item_to_course(@course, active: true) %>
    </ol>
  </div>
</div>
<div class="row">
  <div class="col-md-12">
    <ul class="nav nav-tabs">
      <li><%= link_to t("activerecord.models.course"), course_path(@course), class: "btn btn-link" %></li>
      <li><%= link_to t("activerecord.models.member"), course_members_path(@course, status: '1'), class: "btn btn-link" %></li>
      <li class="active"><a href="#lesson" data-toggle="tab"><%= t("activerecord.models.lesson") %></a></li>
    </ul>
</div>
</div>
<br />
<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th><%= t("activerecord.attributes.lesson.date") %></th>
            <th><%= t("activerecord.attributes.lesson.status") %></th>
            <th><%= t("activerecord.attributes.lesson.rolls_status") %></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @lessons.each do |lesson| %>
            <tr>
              <td><%= lesson.date %></td>
              <td><%= lesson.status %></td>
              <td><%= lesson.rolls_status %></td>
              <td><%= link_to_show lesson_rolls_path(lesson) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @lessons %>
    </div>
  </div>
</div>

