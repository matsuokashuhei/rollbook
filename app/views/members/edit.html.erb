<% breadcrumb :edit_member, @member %>
<%= render partial: 'nav_tabs', locals: { member: @member, active: :member } %>
<br />
<div class="row">
  <div class="col-md-12">
  <%= render "form" %>
  <%= button_to_save pull: "right" %>
  <%= button_to_back members_path, pull: "left" %>
  </div>
</div>
